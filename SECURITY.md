# zeliji のセキュリティモデル

## 3原則

1. **デフォルトで安全** — 何も設定しなければ、エージェントに許されるのは
   「自分のworktree内のファイル編集 (acceptEdits)」と「`zeliji` / `git`
   コマンドの実行」だけ。任意のシェルコマンドやネットワークアクセスの
   自動承認はない。
2. **危険は明示的に、見える形で** — 権限は3モード:
   `mirror` (読むだけ・提案のみ) / `pair` (既定: worktree内編集+協調コマンド)
   / `rogue` (全自動)。rogueはウィザードで「信頼できる作業場でのみ推奨」と
   確認した上でのオプトインで、有効中はcockpitに **⚠AUTO** バナーが常時
   表示される。
3. **記録は残り、統合は人間** — 各エージェントの全イベントは
   `.zeliji/agents/<role>.jsonl` に保存される。成果がbaseブランチに入るのは
   人間が `zeliji merge` を打った時だけで、自動マージは存在しない。
   コンフリクト時は安全にabortする。

## 実装上の防御

- **役割名の検証**: 役割名はgitブランチ名とディレクトリ名になるため、
  英数と `-` `_` のみ許可 (パス/コマンドインジェクション防止)。設定読み込み
  時とcockpitからの追加時の両方で検証。
- **worktree隔離**: エージェント同士は互いのファイルを直接触れない。
  越境は共有バス (タスク/メッセージ) 経由のみ。
- **バスの排他制御**: flock / msvcrt.locking による排他で、並行書き込みに
  よる破損を防ぐ。
- **`--add-dir` は本体リポジトリのみ**: エージェントの追加アクセスは
  そのリポジトリのルートまで。ホームディレクトリ等には広げない。

## 利用者への注意

- **`include` に指定したファイル (.env等) は各worktreeへコピーされる。**
  エージェントに見せてよいsecretsか確認すること。見せる必要がなければ
  `include` に入れない。
- タスクやメッセージに認証情報を書かない (バスはプレーンファイル)。
- フル自律モードは、壊れても困らない・秘密の少ないリポジトリでのみ使う。
- エージェントの出力・コミットは信頼せずレビューする (だからマージは人間)。

## 報告

脆弱性を見つけたら、公開Issueではなくリポジトリオーナーに直接連絡を。
