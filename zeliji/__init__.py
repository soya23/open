"""zeliji — zellij evolved.

Role-based Claude Code sessions in one zellij layout, connected by a
shared file-based bus (task board + messaging), each isolated in its
own git worktree so parallel edits never conflict on disk.
"""

__version__ = "0.1.0"
